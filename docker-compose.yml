version: '2'

services:
  postgres:
    image: postgres
    user: root
    volumes:
    - './storage/app/postgres/data:/var/lib/postgresql/data'
    environment:
    - POSTGRES_DB=admin
    - POSTGRES_USER=admin
    - POSTGRES_PASSWORD=admin
    - PGDATA=/tmp
    ports:
    - '5432:5432'